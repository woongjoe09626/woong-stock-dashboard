# data 브랜치

스크리너가 매일 올리는 결과만 두는 곳. 여기엔 코드가 없다.

`main`이 아니라 이 브랜치에 두는 이유는, 렌더가 `main`만 보고 있어서
여기에 올려도 사이트가 재배포되지 않기 때문이다. 매일 재배포되면
그때마다 몇 분씩 먹통이 된다.

- `signals-us.json` — 미국장 스크리너 결과 (오늘 신호 + 팔로우 종목 상태)
